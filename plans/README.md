# Implementation Plans

Generated by the improve skill on 2026-08-26 from a focused audit of
`.planning/codebase/CONCERNS.md` (vetted against `src/` at `b87cecb`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Verification baseline for every plan (root package, not examples):

```sh
pnpm test
pnpm lint
pnpm exec tsc --noEmit
pnpm build
```

There is no root `typecheck` script. Lint is `biome lint src`. Do not edit
`dist/` (pkgroll). Do not rewrite example lockfiles unless a plan says so
(`AGENTS.md`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Gate `extractApiInfo` with `shouldScanBuildDirectory` | P1 | M | — | TODO |
| 002 | CLI creates output directories and fails with clear errors | P1 | S | — | TODO |
| 003 | `withSwagger` returns HTTP 500 without leaking error messages | P1 | S | — | TODO |
| 004 | Advertise Node.js `>=20` to match `swagger-jsdoc@6.3.0` | P2 | S | — | TODO |
| 005 | Remove unused `isarray` dependency | P3 | S | — | TODO |
| 006 | Reject invalid `specFile` JSON | P2 | S | — | TODO |
| 007 | Stop globbing all `public/**/*.json` as OpenAPI fragments | P2 | M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001, 002, 003, 004, 005, 006, 007 are independently executable. Prefer
  001 first (build-time correctness), then 002/003 (CLI + handler), then
  006/007 (`src/swagger.ts` glob/`loadSpecFile`), then 004/005 (manifest).
- 001 and 007 both edit `createSwaggerSpec` in `src/swagger.ts` (autoDoc
  call vs `apis` array). If they run in parallel, rebase; do not mix the
  hunks.
- 003 and 006 both edit `src/swagger.ts` (`withSwagger` vs `loadSpecFile`).
  Same rebase caution; they do not share lines at `b87cecb`.
- 003's error-path test uses invalid `specFile` JSON. It stays valid after
  006 (throw message may change; 003 asserts the HTTP body is the constant
  string, not the parse message).
- 002 must not import `src/cli.ts` from tests (cleye runs at load). Tests
  import `src/cli-write.ts`.

## Findings considered and rejected

- Default `apiFolder` `'pages/api'`: documented compatibility (`AGENTS.md`);
  do not change it.
- Richer autoDoc (requestBody, status codes from `Response.json`): stated
  as path + method only.
- `swagger-ui-react` `UNSAFE_componentWillReceiveProps`: not this library.
- Rewriting example lockfiles to local `0.5.0` as part of library bugfixes:
  `AGENTS.md` policy (examples pin published tags). Stale
  `v0.4.2-beta.0` pin is a separate examples chore.
- Coverage thresholds, idle `c8` / `size-limit` / `@skypack/package-check`,
  Biome schema 1.7 vs 1.9, changelog archaeology for 0.3.5–0.4.1.
- Edge runtime support: blocked by `node:fs` + WASM `initSync()`; use
  `specFile` instead.
- Real webpack/turbopack `route.js` fixture: investigate later; current
  compiled fixtures are hand-written ESM.

## Direction not planned

Selected set was table items 1–7 only. Not written:

- App Router JSON Route Handler helper (issue #1190)
- CLI as a fuller postbuild tool (`--apiFolder`, watch, YAML)
- Bump examples to published `0.5.0`
